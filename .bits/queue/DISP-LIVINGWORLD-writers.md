# DISP-LIVINGWORLD-writers

- srs: SRS-CN-WRITERS-LIVINGWORLD-001
- guild: writers
- champion: Quill
- branch: bits/livingworld
- status: queued
- brief: .bits/srs/SRS-CN-WRITERS-LIVINGWORLD-001.md

Open a code session on `bits/livingworld` and build **SRS-CN-WRITERS-LIVINGWORLD-001** — the writers guild's public living-world floor contract (the `citadel.writers.*` events + `/realm/writers.json` feed the Aincrad game reads). Read the brief above + this repo's `.bits/context.md`. Public-safe only. Self-gate `npm run lint` + `npm test`. Open a PR titled after the SRS.
